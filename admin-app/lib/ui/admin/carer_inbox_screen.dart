import 'package:flutter/material.dart';

class CarerInboxScreen extends StatelessWidget {
  const CarerInboxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Carer Inbox')),
      body: const Center(child: Text('Coming soon — Carer Inbox')),
    );
  }
}