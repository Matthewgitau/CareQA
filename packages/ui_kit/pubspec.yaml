name: ui_kit
description: Shared UI components for the CareQA platform with modern, sleek design
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  equatable: ^2.0.5
  flutter_svg: ^2.0.5
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
</arg_value></tool_call>